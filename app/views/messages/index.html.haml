= render 'users/user_navigation'
- @posts.each do |post|
  .row-fluid
    .span3
      = render :partial => 'posts/post', :object => post
    .span9
      %table.table.table-striped
        = render post.messages

  %hr
